version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # Amplify looks here for the static output
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - frontend/node_modules/**/*
